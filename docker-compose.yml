services:
  arbeitszeit:
    build: .
    container_name: arbeitszeit-app
    ports:
      - "8080:3000"
    volumes:
      - arbeitszeit-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  arbeitszeit-data:
